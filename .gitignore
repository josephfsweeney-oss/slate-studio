# Drive OAuth token, catalog cache, portrait cache. Never commit these.
.slate-studio/
.env

# Service-account keys. One was committed once; never again.
*.json
!package.json
!package-lock.json
!vercel.json

dist/
node_modules/
.DS_Store
